// Therapy Data Tracking List

// Track all words
    // create a stop words list
    // create a function to filter out stop words 

// Give each speaker an ID a label or id

// Give each word an a label or id

// Give each utterance a label or id

// Count frequency of words used by each speaker in each session
    // track total of each word
    // present total (value) for any word in tool tip

// Track frequency of words across all sessions
    // track total
    // present total (value) for the specific word in tool tip

// Track frequency of words per session
    // create a counter that updates when new raw json data session files are added for each patient

// Create a filter function for word frequencies  per most recent 4 session

// link speakers to their own words and utterances 

// Give raw json session files times and dates
    // Track time and date for each session
    // Create visualisation for this 

// Count length of session by using start and end values provided in milliseconds
    // Convert this data to hours and minutes
    // Display the time and date of the session on the tool tips

// Create a descending word frequency list function for each speaker by value
    // create a visualisation function to filter the top words displayed from 5-25 words

// Each session must be its own column of words
    //the filters determines how many words per column

// All tracked data must be converted so that it works with D3-sankey

// Create a function to filter the diagram to display all outputs by speaker

// Create a function to link words by id to the tree diagram